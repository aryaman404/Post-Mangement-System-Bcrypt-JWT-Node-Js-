<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="bg-red-500 px-2 py-2 rounded-md mb-5 inline-block"
          value="Logout"
          >Logout</a
        >
      </div>

      <h5 class="mb-8">Edit your post</h5>
      <form action="/update/<%=post._id%>" method="post">
        <textarea
          name="content"
          placeholder="Whats on your mind ?"
          class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
        >
<%= post.content %></textarea
        >
        <input
          type="submit"
          class="px-2 py-2 bg-yellow-500 text-black block mt-2 w-40 rounded-lg"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
